<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.compdf.mapper.AssetMapper">

    <insert id="insertAssetFlow">
        INSERT INTO asset_flow (id, asset_id, current_assets, changed_assets, type, updated_assets, operator_id, status)
        VALUES (#{id}, #{assetId}, #{asset}, #{assetChange},'allocate_update', #{asset} - #{assetChange}, #{userId}, 1)
    </insert>


    <update id="updateAssetByLeaderId">
        update asset
        set asset = asset - #{asset}
        where leader_id = #{licenseId}
        and status = 1
        and asset >= #{asset}
    </update>

</mapper>